import { DescricaResumidaPipe } from './descrica-resumida.pipe';

describe('DescricaResumidaPipe', () => {
  it('create an instance', () => {
    const pipe = new DescricaResumidaPipe();
    expect(pipe).toBeTruthy();
  });
});
